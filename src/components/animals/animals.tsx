// import React, { useState } from "react";
import { RegisterNewAnimal } from "../register_new_animal/register_new_animal"

export const Animals: React.FC = () => {
  // const [animals,setAnimals]=useState<Animal|undefined>();

  return (
    <>
      <RegisterNewAnimal />
    </>
  )
}
